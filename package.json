{
  "name": "bookverse",
  "version": "1.0.0",
  "description": "+ Create a new directory for your project and initialize a new Node.js project by running ```npm init -y```.\r + Install the required dependencies by running ```npm install express mysql```.\r + Create an app.js file and set up the Express.js server.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1",
    "mysql": "^2.18.1"
  }
}
